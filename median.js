const inputs = "30 10 5 40 60 15";

const inputsArray = inputs.split(" ");
console.log(inputsArray);
const sortedInputsArray = [];

console.log(sortedInputsArray);
const length = inputsArray.length;

let median = 0;

if (length % 2 === 0) {
}
